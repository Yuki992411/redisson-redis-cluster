apiVersion: v1
kind: Service
metadata:
  name: redisson-redis-cluster
  labels:
    app: redisson-redis-cluster
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: redisson-redis-cluster